services:
  ersatztv:
    image: ghcr.io/ersatztv/ersatztv
    container_name: ersatztv
    restart: always
    environment:
      - TZ=America/Toronto
    ports:
      - "8409:8409"
    volumes:
      - ./config:/config
      - "${USERPROFILE}/Downloads:/downloads:ro"
